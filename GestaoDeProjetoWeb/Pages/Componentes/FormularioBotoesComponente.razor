<div class="col-12 grupo-Botoes">
    <button type="button" class="botao-Customizado botao-Customizado-pesquisar" @onclick="OnCancel">
        <ion-icon class="botao-Customizado-Icone"  name="exit-outline"></ion-icon>
        Cancelar
    </button>
    <button type="submit" class="botao-Customizado botao-Customizado-inserir">
        <ion-icon class="botao-Customizado-Icone"  name="save-outline"></ion-icon>
        Salvar
    </button>
</div>

@code {
    [Parameter] public EventCallback OnCancel { get; set; }
    [Parameter] public EventCallback OnSave { get; set; }

    private async Task HandleCancel()
    {
        if (OnCancel.HasDelegate)
        {
            await OnCancel.InvokeAsync();
        }
    }

    private async Task HandleSave()
    {
        if (OnSave.HasDelegate)
        {
            await OnSave.InvokeAsync();
        }
    }
}