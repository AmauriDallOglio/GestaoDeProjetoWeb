<div class="adicionaEspaco  tabela-Largura adicionarCorInterna">
    <div class="col-md-auto ">
 
@*         @if (HabilitaBotaoAlterar)
        {
            <button class="adicionarAjusteBotaoFiltro botao-Customizado botao-Customizado-alterar" @onclick="OnAlterar">
                <ion-icon name="create-outline"></ion-icon>
                Alterar
            </button>
        }

        @if (HabilitaBotaoExcluir)
        {
            <button class="adicionarAjusteBotaoFiltro botao-Customizado botao-Customizado-excluir" @onclick="OnExcluir">
                <ion-icon name="trash-outline"></ion-icon>
                Excluir
            </button>
        }

        @if (HabilitaBotaoFormulario)
        {
            <button class="adicionarAjusteBotaoFiltro botao-Customizado botao-Customizado-pagina @(!HabilitaBotaoFormulario ? "btn-escondido" : "")" @onclick="OnSquad">
                <ion-icon name="browsers-outline"></ion-icon>
                Squad
            </button>
        } *@

        <button class="botao-Customizado botao-Customizado-alterar  @(!HabilitaBotaoAlterar ? "btn-escondido" : "")" @onclick="OnAlterar">
            <ion-icon class="botao-Customizado-Icone" name="create-outline"></ion-icon>
            Alterar
        </button>
    
        <button class="botao-Customizado botao-Customizado-excluir @(!HabilitaBotaoExcluir ? "btn-escondido" : "" )" @onclick="OnExcluir">
            <ion-icon class="botao-Customizado-Icone" name="trash-outline"></ion-icon>
            Excluir
        </button>

        <button class="botao-Customizado botao-Customizado-pagina @(!HabilitaBotaoFormulario ? "btn-escondido" : "")" @onclick="OnSquad">
            <ion-icon class="botao-Customizado-Icone" name="browsers-outline"></ion-icon>
            Squad
        </button>

    </div>
</div>

@code {
    [Parameter] public bool HabilitaBotaoAlterar { get; set; }
    [Parameter] public EventCallback OnAlterar { get; set; }
    [Parameter] public bool HabilitaBotaoExcluir { get; set; }
    [Parameter] public EventCallback OnExcluir { get; set; }
    [Parameter] public bool HabilitaBotaoFormulario { get; set; }
    [Parameter] public EventCallback OnSquad { get; set; }
}